# hello-world-sidecar-dotnet

Simple .NET sidecar example for Valtren AI.

## What it demonstrates

- `/health`
- `/hooks/{hookName}`
- minimal ASP.NET Core sidecar wiring

## Local run

```bash
cd examples/dotnet/hello-world-sidecar-dotnet
dotnet run
```
